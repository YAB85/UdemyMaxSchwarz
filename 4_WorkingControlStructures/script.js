/* function shortcut(string) {
	let x = string.split('');
	let newString;
	for (let i = 0; i < x.length; i++) {
		if (x[i] === 'o' || x[i] === 'e' || x[i] === 'i' || x[i] === 'a' || x[i] === 'u') {
			delete x[i];
		}
		newString = x.join('');
	}
	return newString;
}

console.log(shortcut('how are you today?'));
//1. получить массив
// 2. удалить гласные a e o i
x.join();
x.split(); */